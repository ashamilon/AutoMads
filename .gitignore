node_modules/
dist/
.env
.env.*
!.env.example
*.log
LOCAL-TENANT-API-KEY.txt
persona.local.json
.DS_Store
data/training-json/
client/.next/
client/node_modules/
client/.env.local
!client/.env.local.example
scripts/
inbox/
all_messages/
*.sqlite
*.db
# Diagnostic dumps (contain customer PSIDs, phone numbers, addresses)
.tmp/
# Generated PDF invoices (contain real customer data)
public/invoices/
# Per-tenant uploaded media (logos, brand images). These get written at
# runtime; production deploys should host them in object storage. Keeping
# them out of git so customer media doesn't end up in commits.
public/tenant-assets/
